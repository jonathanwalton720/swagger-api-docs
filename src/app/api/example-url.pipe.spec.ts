import { ExampleUrlPipe } from './example-url.pipe';

describe('ExampleUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ExampleUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
